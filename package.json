{
  "name": "rele",
  "private": true,
  "workspaces": [
    "db",
    "gate",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --filter web dev\" \"bun run --filter gate dev\"",
    "dev:gate": "bun run --filter gate dev",
    "build:gate": "bun run --filter gate build",
    "start:gate": "bun run --filter gate start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
